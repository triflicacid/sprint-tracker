# Ideas backlog

This document is ideas only.
If an idea has been turned into a plan under `plans/`, or has already been implemented, remove it from this file.


## Enhancements

- `/transitions` - all as SVG and separated arrows so they are legible. Some arrows can be a straight line between lozenges if 1. they are next to one-another and 2. there is not already a straight connection (avoid overlap)

## Bugs

- `jira_labels` is a TEXT, shall we use tags for this instead of a separate field